rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write for OCT scan rasters, segmentation masks, overlays, and PDF reports
      allow read, write: if true;
    }
  }
}
