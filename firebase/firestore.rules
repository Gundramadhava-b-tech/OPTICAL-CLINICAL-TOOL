rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Retinal scans and audit records
      allow read, write: if true;
    }
  }
}
